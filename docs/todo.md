# Human TODO

Don't implement. It is under discussion and may be removed.

- SQLite FTS and SQLite encryption
- rclone support
- full config editor and help + hint system in GUI
- fix release:
  ```
  ubuntu@2025-2-suzuki-plus-one:~$ freebooru server start
  Failed to connect to bus: No medium found
  run systemctl: exit status 1
  ```
- gif autoplay
- document preview in thumbnail view
- download button for any file
- number of files for each tag under search
- upload local anime pack
- ensure that freebooru init runs automatically on installation
- on tag assign double click assign button click is required
- server exposes file upload endpoint eg cat img.png | freebooru to "<upload_endpoint>"
- file watch: add tag ui bug, edit tags ui bugs + feat: copy hash on click
- because of bad hints, cant query by metatags like last_interaction_at. they are also not shown in the tag list of the image.
- start from all files page on frontend. fix hot search, fix file download button for any file, fix tag edition for file.
- generate a decent example configuration
- file page: add tag with seach and fix edit + file name edition
- after uploading a file, just go to the file page or search bar
- open all files not overiview by default
- under tags page, we need a editable graph view. like this shit depends on this actually. and we need to be able to edit the graph. like add a tag, remove a tag, change a tag type, change a tag group, change a tag requirement, etc.
- consider manual rewriting. code quality is fine enough, but i want freebooru to be the hobby project. like coding as hobby ya know

